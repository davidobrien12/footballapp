<%= form_with(model: ticket, local: true) do |form| %>
<body style="background-color:#3498DB;">
  <% if ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
      <% ticket.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :fixture_id %>
    <%= form.number_field :fixture_id, id: :ticket_fixture_id %>
  </div>

  <div class="field">
    <%= form.label :seat_type %>
    <%= form.text_field :seat_type, id: :ticket_seat_type %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :ticket_price %>
  </div>

  <div class="field">
    <%= form.label :no_of_tickets %>
    <%= form.number_field :no_of_tickets, id: :ticket_no_of_tickets %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
</body>
