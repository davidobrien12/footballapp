<p id="notice"><%= notice %></p>
<h2><%= link_to 'Home', teams_path %>
</h2>
<h1>Fixtures</h1>

<body style="background-color:#3498DB;">

<table>
  <thead>
    <tr>
      <th>Team</th>
      <th>Away team</th>
      <th>Stadium</th>
      <th>Time</th>
      <th>Date</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @fixtures.each do |fixture| %>
      <tr>
        <td><%= fixture.team.team_Name %></td>
        <td><%= fixture.away_team %></td>
        <td><%= fixture.stadium %></td>
        <td><%= fixture.time %></td>
        <td><%= fixture.date %></td>
        <td><%= link_to 'Show', fixture %></td>
        <td><%= link_to 'Edit', edit_fixture_path(fixture) %></td>
        <td><%= link_to 'Destroy', fixture, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
</body>

<%= link_to 'New Fixture', new_fixture_path %>
